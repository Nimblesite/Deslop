{
  "name": "deslop-vscode",
  "displayName": "Deslop",
  "description": "Finds, ranks, and helps you fix duplicated code as you type — detection shipping today, AI-assisted and mechanical refactors next.",
  "version": "0.1.0",
  "publisher": "deslop",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/MelbourneDeveloper/CodeDedup.git",
    "directory": "clients/vscode"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Linters",
    "Programming Languages",
    "Other"
  ],
  "keywords": [
    "duplication",
    "clone detection",
    "refactoring",
    "code quality",
    "DRY"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onLanguage:csharp",
    "onLanguage:rust",
    "onLanguage:python",
    "workspaceContains:**/*.cs",
    "workspaceContains:**/*.rs",
    "workspaceContains:**/*.py"
  ],
  "contributes": {
    "commands": [
      {
        "command": "deslop.openReport",
        "title": "Deslop: Open Report"
      },
      {
        "command": "deslop.openWorstCluster",
        "title": "Deslop: Open Worst Cluster"
      },
      {
        "command": "deslop.jumpToNextOccurrence",
        "title": "Deslop: Jump to Next Occurrence in Cluster"
      },
      {
        "command": "deslop.compareWithCanonical",
        "title": "Deslop: Compare With Canonical Occurrence"
      },
      {
        "command": "deslop.pickEmbeddingModel",
        "title": "Deslop: Pick Embedding Model"
      },
      {
        "command": "deslop.refreshReport",
        "title": "Deslop: Refresh Report (force full re-analysis)"
      },
      {
        "command": "deslop.toggleShowAllLenses",
        "title": "Deslop: Toggle Show All Code Lenses"
      },
      {
        "command": "deslop.showSchemaDoc",
        "title": "Deslop: Show Schema Documentation"
      },
      {
        "command": "deslop.openCluster",
        "title": "Deslop: Open Cluster"
      },
      {
        "command": "deslop.revealActiveBinary",
        "title": "Deslop: Reveal Active Binary"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "deslop",
          "title": "Duplicate Clusters",
          "icon": "media/activity-bar.svg"
        }
      ]
    },
    "views": {
      "deslop": [
        {
          "id": "deslop.topOffenders",
          "name": "Top Offenders"
        },
        {
          "id": "deslop.focusedFile",
          "name": "Focused File"
        },
        {
          "id": "deslop.session",
          "name": "Session"
        }
      ]
    },
    "configuration": {
      "title": "Deslop",
      "properties": {
        "deslop.minNodes": {
          "type": "integer",
          "default": 30,
          "minimum": 1,
          "description": "Minimum AST node count for a subtree to be fingerprinted. Forwarded to the LSP at initialize."
        },
        "deslop.embedding.provider": {
          "type": "string",
          "enum": [
            "ollama",
            "stub"
          ],
          "default": "ollama",
          "description": "Embedding provider id."
        },
        "deslop.embedding.model": {
          "type": "string",
          "default": "nomic-embed-text",
          "description": "Embedding model id. Change via the picker; this value is persisted."
        },
        "deslop.embedding.endpoint": {
          "type": "string",
          "default": "http://127.0.0.1:11434",
          "description": "Ollama HTTP endpoint. Loopback-only by default."
        },
        "deslop.embedding.mode": {
          "type": "string",
          "enum": [
            "auto",
            "required",
            "off"
          ],
          "default": "auto",
          "description": "Mirrors CLI --embeddings={auto,required,off}."
        },
        "deslop.incremental": {
          "type": "boolean",
          "default": true,
          "description": "Enable on-disk fingerprint cache (.codededup-cache)."
        },
        "deslop.showAllLenses": {
          "type": "boolean",
          "default": false,
          "description": "Show code lenses below the 50th-percentile weight threshold."
        },
        "deslop.configPath": {
          "type": "string",
          "default": "",
          "description": "Optional path to .codededup.toml. Mirrors CLI --config."
        },
        "deslop.liveBubble.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Show the live duplication bubble as you type."
        },
        "deslop.liveBubble.mode": {
          "type": "string",
          "enum": [
            "inline",
            "ghost"
          ],
          "default": "inline",
          "description": "Bubble layout: inline decoration + inlay hint, or a phantom ghost line below the range."
        }
      }
    },
    "mcpServers": [
      {
        "id": "deslop",
        "name": "Deslop",
        "description": "Live duplicate-code analysis for the current workspace.",
        "command": "${extensionPath}/bin/${platform}/deslop-mcp",
        "args": [],
        "env": {}
      }
    ]
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit",
    "compile": "tsc -p ./",
    "pretest": "npm run compile && cp -r src/test/fixtures out/test/fixtures",
    "test": "vscode-test",
    "coverage": "npm run pretest && vscode-test --coverage && node ./scripts/check-coverage.mjs",
    "package": "npm run build && vsce package --no-dependencies -o deslop-vscode.vsix"
  },
  "dependencies": {
    "pino": "^10.3.1",
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.9",
    "@types/node": "^20.12.0",
    "@types/vscode": "^1.90.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.4.1",
    "@vscode/vsce": "^3.1.1",
    "c8": "^10.1.2",
    "esbuild": "^0.25.0",
    "glob": "^11.0.0",
    "mocha": "^10.8.2",
    "typescript": "^5.6.3"
  }
}
