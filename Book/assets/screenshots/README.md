# Product screenshots

No product screenshots are included in the structural scaffold. Direct captures will be added after the edition release is pinned in `book.json`.

Untouched masters belong in `assets/screenshots/masters/`. Every capture must record its environment, artifact hash, fixture, source file, and capture method in `figures.json`.
