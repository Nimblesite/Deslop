# agent-pmo:9a71cbf
# =============================================================================
# Universal — applies to every repo
# =============================================================================

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# IDE / Editor
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/
*.sublime-project
*.sublime-workspace

# Portfolio-wide tooling
.too_many_cooks/
.commandtree/
.playwright-mcp/
coordination/
logs/
nohup.out

# Coverage artifacts
coverage/
lcov.info
*.profraw
*.profdata

# Secrets / local overrides
.env
.env.local
.env.*.local
*.local
*.secret
*.pem
*.key
!*.pub.key

# Temporary
tmp/
temp/
scratch/

# =============================================================================
# Rust
# =============================================================================
/target/
Cargo.lock.bak

# =============================================================================
# Gradle / IntelliJ Platform
# =============================================================================
clients/jetbrains/.gradle/
clients/jetbrains/.intellijPlatform/
clients/jetbrains/build/

codededup-report.json
codededup-report.txt
codededup-report.html
codededup-*.log


node_modules/
site/_site/

/*.png

/clients/vscode/.vscode-test/
/clients/vscode/dist
clients/vscode/out/

.deslop-cache/
# Test fixtures need their pre-baked state file + embeddings checked in so
# StateFileBackend has data to serve on a fresh CI checkout. Re-include the
# directory and its contents.
!crates/deslop-mcp/tests/fixtures/csharp-mcp/.deslop-cache/
!crates/deslop-mcp/tests/fixtures/csharp-mcp/.deslop-cache/**

# typeDiagram-generated IPC model code (source of truth: docs/models/*.td).
# Per CLAUDE.md the generated wire types must NOT be committed; they are
# regenerated on every cargo build by `crates/deslop-core/build.rs` and
# on every `make vsix-build` via the `typediagram-gen` Makefile target.
crates/deslop-core/src/wire_generated.rs
clients/vscode/src/types/wire-generated.ts



/*.png

*.vsix


.ghissues/
clients/vscode/bin/

clients/vscode/media/webview/

crates/deslop-mcp/tests/fixtures/csharp-mcp/.deslop-cache/
